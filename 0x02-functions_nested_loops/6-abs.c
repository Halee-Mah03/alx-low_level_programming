#include "main.h"

/**
 * _abs - to compute absolute integer
 *
 * @r: the integer neded for computing
 *
 * Return: the absolute integer
 */

int _abs(int r)
{
	if (r >= 0)

		return (r);
	else
		return (-r);
}
