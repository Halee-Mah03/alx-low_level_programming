#include "main.h"

/**
 * _abs - to compute absolute integer
 *
 * @n: the integer to be checked
 *
 * Return: the absolute integer
 */

int _abs(int)
{
	if (n >= 0)

		return (n);
	else
		return (-n);

}
